# CaptRAW Documents 第4阶段开发计划

---

## 🎯 核心目标

1. **重新建立存储桶** - 创建干净的对象存储环境
2. **建立PostgreSQL数据库** - 作为前端和存储桶的元数据桥梁
3. **迁移示例数据** - 将文档、公告、横幅迁移到存储桶
4. **清理模拟后端** - 移除占位字符和示例数据
5. **实现通用S3兼容** - 支持多种对象存储运营商

---

## 📋 详细任务清单

### 阶段 1: 数据库设计与初始化 (预计时间: 2-3小时)

#### 1.1 设计数据库表结构
**文件**: `backend/prisma/schema.prisma` 或 `backend/src/database/schema.sql`

**核心表设计**:
- **users** - 用户表（后续扩展）
- **articles** - 文章表
- **notices** - 公告表  
- **banners** - 横幅表
- **tags** - 标签表
- **comments** - 评论表
- **assets** - 资产元数据表（核心）

**资产表结构示例**:
```sql
CREATE TABLE assets (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  uuid VARCHAR(255) NOT NULL UNIQUE,      -- 前端使用的 assetUuid
  kind VARCHAR(50) NOT NULL,              -- 'user' | 'article' | 'notice' | 'banner'
  object_key VARCHAR(500) NOT NULL,       -- 存储桶中的对象键
  mime_type VARCHAR(100),                 -- 文件类型
  size_bytes BIGINT,                      -- 文件大小
  visibility VARCHAR(20) DEFAULT 'public', -- 'public' | 'private'
  owner_id VARCHAR(255),                  -- 所有者ID（用户ID）
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 1.2 更新数据库初始化脚本
**文件**: `backend/init-db.sql`
- 创建所有表结构
- 插入基础示例数据
- 创建索引和约束

#### 1.3 配置数据库连接
**文件**: `backend/src/database/`
- 创建数据库连接池
- 实现基础CRUD操作
- 集成到现有配置系统

### 阶段 2: 存储桶环境准备 (预计时间: 1-2小时)

#### 2.1 清理现有存储桶
**方法**:
- 停止并删除MinIO容器
- 删除MinIO数据卷
- 重新启动干净的MinIO服务

**命令示例**:
```bash
docker-compose down -v
docker-compose up -d minio
```

#### 2.2 验证存储桶访问
- 测试MinIO控制台访问 (localhost:9001)
- 验证API端点连通性
- 测试基础存储操作

#### 2.3 创建存储桶结构
- 创建标准目录结构: `user/`, `article/`, `notice/`, `banner/`
- 设置适当的访问权限
- 配置生命周期策略（可选）

### 阶段 3: 通用S3客户端集成 (预计时间: 2-3小时)

#### 3.1 评估S3客户端选项
**选项分析**:
- **aws-sdk/client-s3**: 官方SDK，功能完整，体积较大
- **minio**: 轻量级，专为MinIO优化，S3兼容
- **@aws-sdk/client-s3**: 模块化，现代，推荐选择

**决定**: 使用 `@aws-sdk/client-s3` 以获得最佳兼容性

#### 3.2 安装依赖
```bash
cd backend
npm install @aws-sdk/client-s3 @aws-sdk/s3-request-presigner
npm uninstall minio  # 移除minio依赖
```

#### 3.3 重构S3客户端模块
**新文件**: `backend/src/storage/s3Client.ts`
```typescript
import { S3Client, GetObjectCommand } from '@aws-sdk/client-s3';
import { getSignedUrl } from '@aws-sdk/s3-request-presigner';

export class S3StorageClient {
  private client: S3Client;
  
  constructor(config: S3Config) {
    this.client = new S3Client({
      endpoint: config.endpoint,
      region: config.region,
      credentials: {
        accessKeyId: config.accessKey,
        secretAccessKey: config.secretKey,
      },
      forcePathStyle: true, // MinIO需要
    });
  }
  
  async generateSignedUrl(bucket: string, key: string, expiresIn: number): Promise<string> {
    const command = new GetObjectCommand({ Bucket: bucket, Key: key });
    return getSignedUrl(this.client, command, { expiresIn });
  }
  
  // 其他方法：upload, delete, list等
}
```

#### 3.4 更新配置结构
**修改**: `backend/src/config.ts`
- 统一S3配置字段命名
- 添加通用S3配置选项
- 保持向后兼容

### 阶段 4: 资产迁移与上传 (预计时间: 2-3小时)

#### 4.1 准备示例资产
**源文件**: `Reference/resource/useravatar-1024x1024.png`
- 转换为WebP格式（可选）
- 生成不同尺寸的缩略图
- 准备其他示例图片（文章封面、横幅等）

#### 4.2 创建资产上传脚本
**文件**: `backend/scripts/upload-assets.ts`
- 读取示例数据文件 (`frontend/src/data/`)
- 上传图片到存储桶
- 记录元数据到数据库
- 生成assetUuid映射关系

#### 4.3 更新示例数据
**修改文件**:
- `frontend/src/data/articles.json` - 更新封面assetUuid
- `frontend/src/data/banners.json` - 更新图片assetUuid  
- `frontend/src/data/notices.json` - 更新作者头像assetUuid

### 阶段 5: 后端服务重构 (预计时间: 3-4小时)

#### 5.1 重构资产注册系统
**替换**: `backend/src/assetRegistry.ts`
- 从数据库查询资产记录
- 实现权限检查逻辑
- 保持现有API兼容性

#### 5.2 更新签名路由
**修改**: `backend/src/assetSignRoute.ts`
- 集成数据库查询
- 使用新的S3客户端
- 优化错误处理

#### 5.3 清理模拟数据
**移除**:
- `backend/src/assetMockMap.ts`
- 硬编码的ASSET_REGISTRY
- 占位符URL生成逻辑

#### 5.4 添加管理端点
**新端点**:
- `POST /api/assets/upload` - 文件上传
- `GET /api/assets/:uuid` - 获取资产信息
- `DELETE /api/assets/:uuid` - 删除资产

### 阶段 6: 配置系统优化 (预计时间: 1-2小时)

#### 6.1 统一配置字段
**更新**: 根目录 `config.yaml`
```yaml
storage:
  type: s3  # s3 | minio | azure | gcs
  endpoint: http://localhost:9000
  bucket: Capt-Docs-Storage
  accessKey: Capt-Docs
  secretKey: 12345678
  region: us-east-1
  publicBaseUrl: http://localhost:9000/Capt-Docs-Storage
```

#### 6.2 更新配置同步脚本
**修改**: `frontend/scripts/sync-config.js`
- 同步新的配置结构
- 保持前端配置去敏
- 验证配置完整性

#### 6.3 更新前端配置类型
**修改**: `frontend/src/config/runtimeConfig.ts`
- 更新类型定义
- 确保类型安全
- 添加配置验证

### 阶段 7: 前端集成测试 (预计时间: 1-2小时)

#### 7.1 验证资产加载
- 测试横幅图片加载
- 验证文章封面显示
- 检查用户头像渲染

#### 7.2 测试签名URL流程
- 验证单个资产签名
- 测试批量签名请求
- 检查URL过期机制

#### 7.3 性能与错误处理
- 测试网络错误回退
- 验证缓存机制
- 检查加载状态显示

---

## 🚀 实施优先级

### 高优先级 (必须完成)
1. **数据库表创建** - 所有功能的基础
2. **S3客户端集成** - 通用存储访问
3. **资产上传脚本** - 数据迁移

### 中优先级 (重要优化)  
4. **配置系统统一** - 简化配置管理
5. **后端服务重构** - 清理技术债务

### 低优先级 (锦上添花)
6. **前端配置更新** - 用户体验优化
7. **详细文档编写** - 便于后续维护

---

## 🔧 技术决策要点

### 1. 数据库选择
- 使用 **PostgreSQL** 作为主数据库
- 使用 **Prisma ORM** 进行数据访问（后续集成）
- 当前阶段使用原始SQL以保持简单

### 2. 存储协议
- 使用 **S3协议** 作为标准接口
- 支持 **MinIO, AWS S3, 阿里云OSS** 等通用对象存储兼容实现
- 通过配置切换不同存储后端

### 3. 资产标识
- 保持现有的 **assetUuid** 系统
- 数据库存储真实的对象键映射
- 前端不感知实际存储路径

### 4. 安全性
- 签名URL有效期设为 **5分钟**
- 私有资产需要 **用户认证**
- 配置信息 **隔离敏感字段**

---

## 📊 预期成果

### 功能层面
- ✅ 干净的MinIO存储桶环境
- ✅ 完整的PostgreSQL数据库结构  
- ✅ 所有示例数据迁移到存储桶
- ✅ 通用的S3兼容存储访问
- ✅ 清理后的模拟后端代码

### 技术层面
- ✅ 统一的配置管理系统
- ✅ 类型安全的资产访问
- ✅ 优化的性能表现
- ✅ 良好的错误处理机制

### 质量层面
- ✅ 通过TypeScript编译检查
- ✅ 保持现有功能完整性
- ✅ 前后端集成测试通过
- ✅ 配置变更无需代码修改

---

## 🧪 验证清单

### 数据库验证
- [ ] PostgreSQL容器正常运行
- [ ] 所有表创建成功
- [ ] 示例数据插入正确
- [ ] 索引和约束生效

### 存储验证
- [ ] MinIO控制台可访问
- [ ] 存储桶创建成功
- [ ] 示例文件上传完成
- [ ] 签名URL生成正常

### 后端验证
- [ ] 服务启动无错误
- [ ] 资产查询接口正常
- [ ] 签名URL接口正常
- [ ] 错误处理机制完善

### 前端验证
- [ ] 配置加载成功
- [ ] 所有图片正常显示
- [ ] 懒加载功能正常
- [ ] 主题切换无影响

---

## ⚠️ 风险与应对

### 技术风险
1. **S3兼容性问题**
   - **应对**: 使用aws-sdk官方库，配置forcePathStyle

2. **数据库迁移数据丢失**
   - **应对**: 先备份现有数据，使用事务操作

3. **配置同步失败**
   - **应对**: 保持默认配置，提供详细错误日志

### 时间风险
1. **任务依赖复杂**
   - **应对**: 按阶段顺序执行，每个阶段可独立验证

2. **集成测试耗时**
   - **应对**: 编写自动化测试脚本（仅用于测试，实际生产环境必须交由用户手动配置），并行验证

### 兼容性风险
1. **现有功能破坏**
   - **应对**: 保持API接口不变，逐步替换内部实现

---

## 📈 成功指标

### 量化指标
- **存储桶文件数**: ≥ 20个示例文件
- **数据库记录数**: ≥ 30条资产记录
- **API响应时间**: < 200ms (本地环境)
- **配置加载时间**: < 100ms
- **代码覆盖率**: ≥ 70% (核心模块)

### 质量指标
- **零编译错误**: TypeScript严格模式通过
- **功能完整性**: 所有现有功能保持正常
- **配置安全性**: 敏感配置无泄露风险
- **文档完整性**: 所有变更有对应文档

### 用户体验指标
- **图片加载成功率**: 100%
- **页面加载性能**: 无显著下降
- **错误处理友好性**: 有明确的用户提示

---


## 📚 参考资料

### 项目文档
- `20260330项目企划书.plan.md` - 原始项目规划
- `20260330第一次项目进度总结.md` - 第一阶段总结
- `20260331第二次项目进度总结.md` - 数据重构总结
- `20260331第三次项目进度总结.md` - 配置系统总结

### 技术文档
- **PostgreSQL文档**: https://www.postgresql.org/docs/
- **AWS S3 SDK文档**: https://docs.aws.amazon.com/sdk-for-javascript/
- **MinIO文档**: https://min.io/docs/
- **Docker Compose文档**: https://docs.docker.com/compose/

### 配置示例
- **S3配置示例**: 参考AWS官方文档
- **数据库连接示例**: 参考Prisma文档
- **环境变量管理**: 参考12-factor原则

---

## 🔄 迭代计划

### 第一次迭代
- 数据库表结构创建
- 存储桶环境准备
- S3客户端基础集成

### 第二次迭代
- 资产上传脚本完成
- 后端服务重构
- 基础集成测试

### 第三次迭代
- 配置系统统一
- 性能优化调整
- 详细文档编写

---

## 💡 注意事项

1. **保持向后兼容**: 现有前端代码不应受影响
2. **配置驱动开发**: 所有行为由配置控制
3. **类型安全优先**: TypeScript严格模式
4. **错误处理完善**: 用户友好的错误提示
5. **文档同步更新**: 代码变更伴随文档更新

---

## 🏁 完成标志

当以下所有条件满足时，本阶段计划完成：

1. ✅ 数据库表结构创建并验证
2. ✅ 存储桶环境就绪并测试
3. ✅ 所有示例数据迁移完成
4. ✅ S3客户端集成测试通过
5. ✅ 前后端集成验证通过
6. ✅ 配置系统更新完成
7. ✅ 相关文档更新完成